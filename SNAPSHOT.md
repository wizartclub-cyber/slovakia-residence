# SNAPSHOT.md — стан на 2026-09-08

**Де ми:** Slice 6 зроблено частково (доступність, реліз, карта сайту). **17 із 26 джерел мають збережену копію з SHA-256.** Наступний крок: шість файлів MZV зберегти з браузера (RUN_ME.md), далі — чеклисти документів і підказки по полях бланків.

**Живий сайт:** https://wizartclub-cyber.github.io/slovakia-residence/ (деплой автоматичний при пуші в main)

**Що працює:** дві мови в адресі (`/uk/`, `/sk/`) · дисклеймер і рядок «Стан права: 01.09.2026 · юридичне рев'ю не завершене» · сторінки Головна / Знайти маршрут / Джерела / Про проєкт / Приватність / 404 · сторінка «Джерела» з 26 записами, датами перевірки та статусами · опитувальник із 5 питань і екран результатів · сторінки маршрутів із друком на A4 · 180 unit + 113 e2e тестів, WCAG 2.2 AA чистий (axe).

**Движок правил:** `src/lib/rules/` — чисті функції, тризначна логіка (є / немає / не знаю), п'ять результатів (`not_applicable`, `excluded`, `not_eligible`, `possible`, `eligible`). Маршрутів у даних ще немає — движок перевірено на синтетичних фікстурах.

**Дані в `content/`:** 26 джерел (17 зі збереженою копією і SHA-256) · 1 поріг (životné minimum 295.22) · 4 органи · 25 позицій зборів · **7 маршрутів-вказівників** (B1 §22, B2 §23, B6 §27, C3 §46, C4 §52, A3 віза, E7 перехід). Усе зі статусом `draft`, у маршрутів `conditionsComplete: false` — юридичного рев'ю не було.

**Гейти (падає збірка):** немає `sourceIds` · джерело не з реєстру · `published`/`legally_reviewed` без `reviewer` · ключ є в uk і немає в sk · невідоме поле в yaml · `validTo` раніший за baseline · орган без `checkedAt`.

**Команди:** `pnpm dev` → http://localhost:5173/slovakia-residence/ · `pnpm validate` · `pnpm test` · `pnpm build` · `pnpm test:e2e` · `pnpm sources:build` (перегенерувати `content/sources/` з реєстру)

**Що зараз стримує проєкт:**
1. Шість файлів MZV (обидва бланки, дві візові анкети, дві сторінки роз'яснень) — сайт блокує програми, зберегти з браузера і запустити `python3 scripts/download_and_hash.py --local`. Без них немає підказок по полях бланків.
2. Три джерела без офіційної адреси: `opatrenie-155-2026`, `slovlex-5-2004`, `minv-hlasenie-pobytu-form`.
3. Юрист — без нього жоден маршрут не стане `legally_reviewed`.

**Блокери:** снапшоти джерел не зняті (власник: `python3 scripts/download_and_hash.py`, див. RUN_ME.md) — доки їх немає, жодне джерело не може стати `source_verified`. Три джерела в реєстрі без url.

**Пам'ятати:** доки в маршруті `conditionsComplete: false`, движок не скаже «підходить» — це запобіжник від хибних обіцянок · маршрут для справ, розпочатих до зміни закону, не може мати `validFrom` = дата зміни (див. PLAN.md) · нова сторінка додається в `content/ui/site.yaml`, інакше GitHub Pages не створить для неї файл · `content/sources/*.yaml` не редагувати руками, правити реєстр і запускати `pnpm sources:build`.

**Не чіпати:** `sources/snapshots/` (генерується скриптом) · `document-registry-v0.3.yaml` (історія) · спека та SOURCE_AUDIT.
